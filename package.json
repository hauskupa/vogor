{
  "name": "vogor-webflow-build",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "npx esbuild src/main.js --bundle --format=iife --target=es2019 --sourcemap --outfile=dist/main.dev.js --watch",
    "release": "node release.js patch",
    "deploy": "npm run build && git add -A && git diff --cached --quiet || git commit -m \"Auto deploy\" && git push origin main && bash ./purge.sh && npm run release",
    "build:purge": "npm run build && bash ./purge.sh",
    "purge": "bash ./purge.sh",
    "build:icons": "svg-sprite-generate -d ./icons -o ./dist/sprite.svg"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  },
  "dependencies": {
    "howler": "^2.2.4",
    "lottie-web": "^5.13.0"
  }
}
